<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/search_by_code"
        android:title="Search by Code"
        android:checkable="true"
        android:checked="true"
        app:showAsAction="never"/>
    <item android:id="@+id/search_by_title"
        android:title="Search by Title"
        android:checkable="true"
        android:checked="true"
        app:showAsAction="never"/>
    <item android:id="@+id/search_by_teacher"
        android:title="Search by Teacher"
        android:checkable="true"
        android:checked="true"
        app:showAsAction="never"/>
    <item android:id="@+id/search_by_room"
        android:title="Search by Room"
        android:checkable="true"
        android:checked="true"
        app:showAsAction="never"/>
</menu>